# Overrides (Auth-API)

- Preserve existing auth token and provider link behavior while introducing governance updates.
- Prefer additive schema changes in Beta and Final phases.
- Do not store raw secrets in logs or events.
